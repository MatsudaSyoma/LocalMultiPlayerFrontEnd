const WebSocket = require('ws');const wss = new WebSocket.Server({ port: 8080 });let ueClient = null;wss.on('connection', ws => {  console.log('Client connected');  ws.on('message', message => {    console.log('Received:', message);    // ブラウザ or UE5 のクライアントからのメッセージ中継    if (message.toString().startsWith('ue:')) {      if (ueClient && ueClient.readyState === WebSocket.OPEN) {        ueClient.send(message.toString().slice(3));      }    } else if (message.toString() === 'register-ue') {      ueClient = ws;      console.log('UE5 registered as receiver');    } else {      // 他のクライアントに送信（例：ブラウザ側へ）      wss.clients.forEach(client => {        if (client !== ws && client.readyState === WebSocket.OPEN) {          client.send(`From Other Client: ${message}`);        }      });    }  });  ws.on('close', () => {    if (ws === ueClient) {      ueClient = null;    }    console.log('Client disconnected');  });});